[project]
name = "secure-mini-claw"
version = "0.1.0"
description = "A security-first mini agent framework inspired by OpenClaw"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "openai",
    "google-genai",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
